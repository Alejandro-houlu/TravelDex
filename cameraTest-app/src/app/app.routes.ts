import { Routes } from '@angular/router';
import { ImageRecComponent } from './Components/CamComponents/image-rec.component';

export const routes: Routes = [
    {path:'', component:ImageRecComponent}
];
